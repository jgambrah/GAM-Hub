'use client';
import { PayoutDashboard } from '@/components/admin/payout-dashboard';

export const dynamic = 'force-dynamic';

export default function PayoutsPage() {
    return <PayoutDashboard />;
}
